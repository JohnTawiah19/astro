#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@astrojs/language-server/bin/server.js" "$@"
  ret=$?
else 
  node  "$basedir/../@astrojs/language-server/bin/server.js" "$@"
  ret=$?
fi
exit $ret
